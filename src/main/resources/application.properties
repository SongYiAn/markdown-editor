# ????
spring.application.name=markdown-editor

# ???????????????
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/markdown_editor?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USER:md_user}
spring.datasource.password=${DB_PASS:md_pass}

# JPA ??
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Token ??????????????
app.auth.token-ttl-minutes=1440
app.collab.op-log-limit=200

# ????????
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

