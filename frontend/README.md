# Markdown Editor Frontend

## Run

```bash
cd D:\\Soft\\Java\\markdown-editor\\frontend
npm install
npm run dev
```

The dev server proxies `/api` and `/ws` to `http://localhost:8080`.

## 运行

```bash
cd D:\\Soft\\Java\\markdown-editor\\frontend
npm install
npm run dev
```

开发服务器会代理 `/api` 与 `/ws` 到 `http://localhost:8080`。
